import numpy as np
from utils import playerlocation

def visualprocess(see):
    #sort the flags by distance, use the closest two flags to calculate the location of player
    #sort the players seen, only record the information of the closest five players


    selflocation = playerlocation(flag1, flag2, )



    return np.concatenate(selfstate, playerslocation, balllocation)




def bodyprocess(body_sense):




